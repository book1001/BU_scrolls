$(document).ready(function () {
  // =================================================
  // 🄋 Apply lettering.js to different elements
  // =================================================
  // ⁉️ $("#words p"): #words > "p" => Select all <p> tags inside the element with the ID "words"
  // .................................................
     $("#letters p").lettering();        // Split into individual letters
     $("#words p").lettering('words');   // Split into words
     $("#lines p").lettering('lines');   // Split into lines
 
  // =================================================
  // 🄋 Array of random values
  // =================================================
  // ⁉️ let: declare a variable (variable: a container that holds a value)
  // ⁉️ let randomTexts: the variable is named randomTexts (the name can be anything)
  // ⁉️ [ ~~ ]: define an array, which is a list that can store multiple values
  // ⁉️ "✿", "❀", "ꕤ": These are the items in the array
  // .................................................
     let randomTexts = ["✿", "❀", "ꕤ"];
     let randomColors = ["#ff0000", "#00ff00", "#0000ff"];
     let randomAudios = ["#audio1", "#audio2", "#audio3"];
 
   
   
 
 
  // =================================================
   
 });
 
 